<mvc:View 
    controllerName="logaligroup.invoice.controller.InvoicesList"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m">
 
 <List id="invoicesList" headerText="{i18n>invoiceListTitle}"
    class="sapUiResponsiveMargin"
    items="{invoices>/Invoices}"
    width="auto">
    <headerToolbar>
        <Toolbar >
            <Title text="{i18n>invoiceListTitle}"/>
            <ToolbarSpacer ></ToolbarSpacer>
            <SearchField width="50%" search="onFilterInvoice"/>
        </Toolbar>
    
    </headerToolbar>

    <items>
                <ObjectListItem id="_IDGenObjectListItem1" title="{invoices>Quantity} x {invoices>ProductName}" 
                    number="{
                        parts: [
                        { 
                            path:'invoices>ExtendedPrice' 
                        },
                        { 
                            path:'currency>/usd'
                        }
                        ],
                        type:'sap.ui.model.type.Currency',
                        formatOptions:{showMeasure : false }
                    }"
                    numberUnit = "{currency>/usd}"
                    numberState="{= ${invoices>ExtendedPrice} > 50 ? 'Success' : 'Error'}">

                    <firstStatus>
                        <ObjectStatus id="_IDGenObjectStatus1" 
                                    text="{
                            path: 'invoices>Status',
                            formatter: '.formatter.invoicesStatus2'
                            }" />
                       
                    </firstStatus>

                </ObjectListItem>
    </items>

 </List>


</mvc:View>    